#pragma once

#include <string>
#include <vector>
#include <tuple>

using std::array;
using std::string;
using std::tuple;
using std::unique_ptr;
using std::vector;

using uint = unsigned int;

// debug
#include "Error.hpp"